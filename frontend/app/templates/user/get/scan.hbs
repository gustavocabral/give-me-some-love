<div class="form-group">
    <label class="col-sm-2 control-label">Scanning</label>

    <div class="col-sm-10">
        {{#if isIOS}}
            {{qrcode-processor readValue=transaction}}
        {{else}}
            {{qrcode-camera readValue=transaction}}
        {{/if}}
    </div>
</div>
